{
  "name": "swanson-worx-booking-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: Swanson Worx estimate -> booking-request API (Supabase + Resend).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.0"
  }
}
